# CNN_Classification
A Convolutional Neural Network (CNN) based Cat vs Dog Image Classification project built using TensorFlow and Keras. The model uses data augmentation, dropout regularization, and convolutional layers to achieve accurate binary image classification.
